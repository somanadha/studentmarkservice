version: 2.1

defaults: &defaults
  APP_NAME: studentmarkservice
  IMAGE_NAME: studentmarkservice-image
  APP_PORT: "8100"
  REGISTRY: docker.io
  IMAGE_TAG: latest

jobs:
  build-and-dockerize:
    docker:
      - image: cimg/openjdk:21.0  # Java 21 image with Maven support
    environment:
      <<: *defaults

    steps:
      - checkout

      # Cache Maven dependencies
      - restore_cache:
          keys:
            - maven-{{ checksum "pom.xml" }}
            - maven-

      # Build the application JAR
      - run:
          name: Build JAR using Maven
          command: |
            mvn dependency:copy-dependencies
            mvn -X package -DskipTests

      - save_cache:
          paths:
            - ~/.m2
          key: maven-{{ checksum "pom.xml" }}

      # Enable remote Docker environment (runs Docker in isolated VM)
      - setup_remote_docker:
          version: 20.10.14

      # Login to Docker Hub
      - run:
          name: Docker Login
          command: |
            echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

      # Build and push Docker image
      - run:
          name: Build and Push Docker Image
          command: |
            docker build -t $REGISTRY/$DOCKERHUB_USERNAME/$IMAGE_NAME:$IMAGE_TAG .
            docker push $REGISTRY/$DOCKERHUB_USERNAME/$IMAGE_NAME:$IMAGE_TAG

      - run:
          name: Display Success Message
          command: echo "✅ Maven build and Docker image pushed successfully!"

workflows:
  version: 2
  build_and_push:
    jobs:
      - build-and-dockerize
